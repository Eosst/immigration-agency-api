# This is an example file. DO NOT commit your actual secrets.
# Copy this file to .env for local development and fill in the values.
# In production (e.g., on Railway), create these as environment variables.

# --- DATABASE (Handled by Railway's DATABASE_URL) ---
# No need to set this manually on Railway if you use their Postgres service.

# --- JWT SECRET ---
# Generate a long, random, secure string for this (e.g., from a password generator)
JWT_SECRET=your_super_secret_jwt_key_here

# --- ADMIN CREDENTIALS ---
# The password should be the BCrypt hash, not the plain text password.
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=your_bcrypt_password_hash_here

# --- STRIPE API KEYS ---
STRIPE_API_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# --- EMAIL SENDER (e.g., Gmail) ---
EMAIL_USERNAME=your.email@gmail.com
EMAIL_PASSWORD=your_gmail_app_password
EMAIL_FROM=your.email@gmail.com

# --- CLOUDINARY URL ---
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name

# --- FRONTEND URL ---
# The URL of your deployed frontend application.
# For Netlify previews, you might need to update this, or handle it in your frontend code.
FRONTEND_URL=http://localhost:3000